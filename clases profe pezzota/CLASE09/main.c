#include "header.h"
#include "string.h"

int main()
{

    //printf("%08d%-30s%02d/%02d/%04d%c%05.2f\n");  ---------LONGITUD VARIABLE-----
    //t_alumno alu;
    //ABRIR TXT EN MODO LECTURA
    //ABRIR BINARIO EN MODO ESCRITURA PARA CREARLO
/*
    char cad_aux[56];
    while(fgets(cad_aux, sizeof(cad_aux), pf))
    {

        //if(cad_aux[54]!='\n')
        if(!strchr(cad_aux, '\n'))
        {
            return 1;
        }
        int r = sscanf(cad_aux, "%8d%30[\n]%2d/%2d/%4d%c%5f",&alu.dni, &alu.apyn, &alu.fing.d,&alu.fing.m, &alu.fing.a, &alu.estado, &alu.promedio);
        fwrite(&alu, sizeof(t_alumno), 1, pf_bin);
        //"%d|%[^|\n]|%d/%d/%d\%c\%f"
        }*/


    return 0;
}
